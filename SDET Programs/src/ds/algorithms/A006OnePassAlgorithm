 Condition:
 1)Used for dutch national problem
 2)used for sorting 0's,1's,2's (only for three values)
 3)uses one traversing for loop
 
 Pseudo Code :
 low = 0, mid = 0,
 high = input.length -1
 
 0 - swap(low,mid) and mid++, low++
 1 - mid++
 2 - swap(mid,high) and high--
