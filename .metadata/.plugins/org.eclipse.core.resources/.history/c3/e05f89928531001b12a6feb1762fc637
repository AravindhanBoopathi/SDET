package ds.arrays.string.revision;

public class P010MovePositiveIntegers {

/*
1) Write a program to move all the positive numbers to end. 

Input: [4, -1, 9, 0, 2, -4, 5]
Output: [-1, 0, -4, 4, 9, 2, 5]

Algorithm: Use 2 Pointer

Pseudo-code:

- currentMin = value at 0th index
- currentMax = value at (input.length - 1)th index

- left pointer = 1th index
- right pointer = (input.length - 2)th index

while(left < right)
if(input[left] < currentMin) - swap currentMin and input[left], currentMin = input[left], break;
if(input[left] > currentMax) - swap currentMax and input[left], currentMax = input[right], break;

*/	





}
